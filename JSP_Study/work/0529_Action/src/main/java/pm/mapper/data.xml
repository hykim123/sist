<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="data">
	<insert id="add" parameterType="pm.vo.DataVO">
		insert into data_t (title, writer, contents) values(#{title}, #{writer}, #{contents})
	</insert>

</mapper>